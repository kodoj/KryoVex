import RunOverview from './runOverview.tsx';

/** Overview reads everything it needs from the store inside `RunOverview` and children. */
export default function overviewContent() {
  return <RunOverview />;
}