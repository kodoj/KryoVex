/** Product release version from root `package.json`, injected by Vite (`vite.config.ts` `define`). */
declare const __KRYOVEX_VERSION__: string;

export const KRYOVEX_RELEASE_VERSION: string = __KRYOVEX_VERSION__;
